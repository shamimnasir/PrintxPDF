# Optional: your own Cloudflare Worker fetcher (see worker/). Leave unset to use public reader proxies.
# VITE_FETCH_PROXY=https://printxpdf-fetch.you.workers.dev
# Optional: sub-path hosting (GitHub Pages sets this automatically in CI)
# VITE_BASE=/PrintxPDF/
