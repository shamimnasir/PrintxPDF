{
  "name": "printxpdf-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "tail": "wrangler tail printxpdf-api --format pretty",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vitest": "^5.0.0",
    "wrangler": "^4.130.0"
  }
}
